{
    "name": "computor_v1",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "start": "echo '✨ ✨ ✨ CRISTINA run 'yarn start-front' and 'yarn start-api' on different terminal windows' ✨ ✨ ✨",
      "start-front": "yarn --cwd ./front start",
      "start-api": "cd api && python3 -m venv venv && venv/bin/pip3 install -r requirements.txt && venv/bin/python3 -m flask run"
    }
  }
  